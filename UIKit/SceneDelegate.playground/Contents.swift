//  SceneDelegate.swift
//
//  Created by Andria Gvaramia on 06.11.24.
//

import UIKit

class SceneDelegate: UIResponder, UIWindowSceneDelegate {
    var window: UIWindow?
    func scene(_ scene: UIScene, willConnectTo session: UISceneSession, options connectionOptions: UIScene.ConnectionOptions) {
        
        //MARK: - სთორიბორდით დაწყება
        
//        guard let windowScene = (scene as? UIWindowScene) else { return }
//        window = UIWindow(windowScene: windowScene)
//        let storyboard = UIStoryboard(name: "first", bundle: nil)
//        let initialViewController = storyboard.instantiateViewController(withIdentifier: "first")
//        window?.rootViewController = initialViewController
//        window?.makeKeyAndVisible()
        
        //MARK: - newVC თი დაწყებდა და მხოლოდ ერთი ფეიჯის გამოყენება
        
//        guard let scene = (scene as? UIWindowScene) else { return }
//        window = UIWindow(windowScene: scene)
//        let VC = newVC()
//        window?.rootViewController = VC
//        window?.makeKeyAndVisible()
        
        //MARK: - newVC თი დაწყებდა და შემდეგ შეგვიძია გადასვლა სხვას ცენაზე
        
//        guard let scene = (scene as? UIWindowScene) else { return }
//        window = UIWindow(windowScene: scene)
//
//        let VC = FirstVC()
//        let navigationVC = UINavigationController(rootViewController: VC)
//
//        window?.rootViewController = navigationVC
//        window?.makeKeyAndVisible()
        
        //MARK: - newVC თი დაწყებდა და დაბლა გაქ განლაგებული რილაკები ნავიგაციისთვის
        
//        guard let scene = (scene as? UIWindowScene) else { return }
//        window = UIWindow(windowScene: scene)
//
//        let VC = TabBarController()
//        let navVC = UINavigationController(rootViewController: VC)
//        window?.rootViewController = navVC
//        window?.makeKeyAndVisible()
    }
}

